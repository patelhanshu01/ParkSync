// DELETED: QueueManager component removed — queue feature deprecated. UI now uses reservation display on layout.

// Kept as a placeholder to avoid breaking imports in older branches.
export default function QueueManagerPlaceholder() { return null; }
